export interface User {
  id: string;
  name: string;
  initials: string;
  email: string;
  careerInterest: string;
  currentFocus: string;
  points: number;
  streakWeeks: number;
  joinedAt: string;
}
